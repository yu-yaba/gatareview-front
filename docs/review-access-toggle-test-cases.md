# レビュー閲覧制限 テストケース

対象: `gatareview-front` / `gatareview-back`  
元仕様: [`review-access-toggle-spec.md`](/Users/kawaiyuya/Desktop/gatareview/gatareview-front/docs/review-access-toggle-spec.md)  
更新日: 2026-03-21

## 1. 目的

`review-access-toggle-spec.md` の仕様を満たすテストケースを、**C1 条件網羅**の観点で整理する。  
本書では、各判定で使う条件が **少なくとも 1 回は真、少なくとも 1 回は偽** になるようにテストを構成する。

## 2. 共通前提

- 制御対象は `/lectures/[id]` の授業詳細ページに表示されるレビュー本文のみ
- 制限 `OFF` のときは、全ユーザーが授業詳細レビュー本文を全文閲覧できる
- 制限 `ON` のときは、`ログイン済み` かつ `reviews_count >= 1` のユーザーのみ全文閲覧できる
- 制限対象ユーザーでも、先頭レビューは全文表示し、2件目以降は本文の一部のみ表示する
- レビュー0件の授業ではロックUIを表示しない
- `ありがとう`、`報告`、`ブックマーク`、本人レビュー編集削除のログイン要件は従来どおり維持する

## 3. テストデータ

### 3.1 授業

- `Lecture A`
  - レビュー 0 件
- `Lecture B`
  - レビュー 1 件
- `Lecture C`
  - レビュー 2 件以上
  - 1件目と2件目で本文が明確に判別できること

### 3.2 ユーザー

- `Guest`
  - 未ログイン
- `User Zero`
  - ログイン済み
  - `reviews_count = 0`
- `User One`
  - ログイン済み
  - `reviews_count = 1`
- `Admin User`
  - ログイン済み
  - `admin? = true`
- `General User`
  - ログイン済み
  - `admin? = false`

## 4. 判定 D1: 授業詳細の閲覧権限

### 4.1 判定条件

- `C1`: `restriction_enabled = true`
- `C2`: ログイン済み
- `C3`: `reviews_count >= 1`

期待される判定:

- `restriction_enabled = false` の場合は常に全文閲覧可能
- `restriction_enabled = true` の場合だけ、`ログイン済み` かつ `reviews_count >= 1` を満たしたとき全文閲覧可能

### 4.2 C1 条件網羅の最小ケース

| ケースID | 制限ON | ログイン済み | `reviews_count >= 1` | 想定ユーザー | 想定結果 |
| --- | --- | --- | --- | --- | --- |
| `TC-D1-01` | 偽 | 偽 | 偽 | `Guest` | 全文閲覧可 |
| `TC-D1-02` | 真 | 偽 | 偽 | `Guest` | 制限対象 |
| `TC-D1-03` | 真 | 真 | 偽 | `User Zero` | 制限対象 |
| `TC-D1-04` | 真 | 真 | 真 | `User One` | 全文閲覧可 |

### 4.3 テスト手順と期待結果

#### `TC-D1-01`

- 観点: 制限 `OFF` のときは未ログインでも全文閲覧できる
- 前提: 制限 `OFF`、`Guest`、`Lecture C`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - 全レビュー本文が全文表示される
  - ロックUIは表示されない

#### `TC-D1-02`

- 観点: 制限 `ON` のとき未ログインは制限対象になる
- 前提: 制限 `ON`、`Guest`、`Lecture C`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - 1件目レビューは全文表示される
  - 2件目以降は本文の一部のみ表示される
  - レビュー投稿導線モーダルを開くボタンが表示される

#### `TC-D1-03`

- 観点: 制限 `ON` のとき `reviews_count = 0` のログインユーザーは制限対象になる
- 前提: 制限 `ON`、`User Zero`、`Lecture C`
- 手順:
  1. ログイン状態で `/lectures/:id` を開く
- 期待結果:
  - 1件目レビューは全文表示される
  - 2件目以降は本文の一部のみ表示される
  - 「1レビューを投稿して閲覧する」導線が表示される

#### `TC-D1-04`

- 観点: 制限 `ON` のとき `reviews_count >= 1` のログインユーザーは全文閲覧できる
- 前提: 制限 `ON`、`User One`、`Lecture C`
- 手順:
  1. ログイン状態で `/lectures/:id` を開く
- 期待結果:
  - 全レビュー本文が全文表示される
  - ロックUIは表示されない

## 5. 判定 D2: レビュー件数による表示分岐

### 5.1 判定条件

- `C4`: `reviews.length === 0`
- `C5`: `reviews.length === 1`
- `C6`: `reviews.length >= 2`
- `C7`: `access.access_granted = true`

### 5.2 C1 条件網羅の最小ケース

| ケースID | レビュー0件 | レビュー1件 | レビュー2件以上 | `access_granted` | 想定結果 |
| --- | --- | --- | --- | --- | --- |
| `TC-D2-01` | 真 | 偽 | 偽 | 偽 | 未投稿UI、ロックUIなし |
| `TC-D2-02` | 偽 | 真 | 偽 | 偽 | 1件目のみ全文、ぼかしなし |
| `TC-D2-03` | 偽 | 偽 | 真 | 偽 | 1件目全文、2件目以降制限表示 |
| `TC-D2-04` | 偽 | 偽 | 真 | 真 | 全文表示 |

### 5.3 テスト手順と期待結果

#### `TC-D2-01`

- 観点: レビュー 0 件授業ではロックUIを出さない
- 前提: 制限 `ON`、任意ユーザー、`Lecture A`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - レビュー未投稿状態の通常UIが表示される
  - ロックUIは表示されない

#### `TC-D2-02`

- 観点: レビュー 1 件授業では不自然なぼかしを出さない
- 前提: 制限 `ON`、制限対象ユーザー、`Lecture B`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - 1件目レビューのみ表示される
  - ぼかし表示は出ない
  - ロックUIが不自然に重ならない

#### `TC-D2-03`

- 観点: レビュー 2 件以上かつ制限対象のときだけ 2 件目以降を制限する
- 前提: 制限 `ON`、制限対象ユーザー、`Lecture C`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - 1件目レビューは全文表示される
  - 2件目以降だけが制限表示になる

#### `TC-D2-04`

- 観点: レビュー 2 件以上でも閲覧権限があれば全文表示する
- 前提: 制限 `ON`、`User One`、`Lecture C`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - 全レビュー本文が全文表示される

## 6. 判定 D3: 管理画面アクセス制御

### 6.1 判定条件

- `C8`: 認証済み
- `C9`: 管理者

### 6.2 C1 条件網羅の最小ケース

| ケースID | 認証済み | 管理者 | 想定ユーザー | 想定結果 |
| --- | --- | --- | --- | --- |
| `TC-D3-01` | 偽 | 偽 | `Guest` | `/auth/signin` にリダイレクト |
| `TC-D3-02` | 真 | 偽 | `General User` | `403 Forbidden` |
| `TC-D3-03` | 真 | 真 | `Admin User` | 管理画面表示 |

### 6.3 テスト手順と期待結果

#### `TC-D3-01`

- 観点: 未ログインアクセス
- 前提: `Guest`
- 手順:
  1. `/admin/review-access` にアクセスする
- 期待結果:
  - `/auth/signin` へリダイレクトされる

#### `TC-D3-02`

- 観点: 非管理者アクセス
- 前提: `General User`
- 手順:
  1. ログイン状態で `/admin/review-access` にアクセスする
- 期待結果:
  - `403 Forbidden` の画面または同等の拒否表示になる

#### `TC-D3-03`

- 観点: 管理者アクセス
- 前提: `Admin User`
- 手順:
  1. ログイン状態で `/admin/review-access` にアクセスする
- 期待結果:
  - 管理画面が表示される
  - 現在の ON / OFF 状態、最終更新日時、最終更新者が表示される

## 7. 判定 D4: 管理画面の更新動作

### 7.1 判定条件

- `C10`: 更新方向が `OFF -> ON`
- `C11`: 更新方向が `ON -> OFF`
- `C12`: 更新成功
- `C13`: 通信成功

### 7.2 C1 条件網羅の最小ケース

| ケースID | `OFF -> ON` | `ON -> OFF` | 更新成功 | 通信成功 | 想定結果 |
| --- | --- | --- | --- | --- | --- |
| `TC-D4-01` | 真 | 偽 | 真 | 真 | ON に更新、成功メッセージ |
| `TC-D4-02` | 偽 | 真 | 真 | 真 | OFF に更新、成功メッセージ |
| `TC-D4-03` | 真 | 偽 | 偽 | 真 | 更新失敗メッセージ |
| `TC-D4-04` | 真 | 偽 | 偽 | 偽 | 通信エラーメッセージ |

### 7.3 テスト手順と期待結果

#### `TC-D4-01`

- 観点: `OFF -> ON` 更新
- 前提: 現在 `OFF`
- 手順:
  1. `制限を ON にする` を押す
  2. 確認ダイアログで実行する
- 期待結果:
  - タイトル `レビュー閲覧制限を有効にしますか？` が表示される
  - 本文 `未ログインユーザーとレビュー未投稿ユーザーは、授業詳細ページのレビュー全文を閲覧できなくなります。` が表示される
  - 更新後に `レビュー閲覧制限を有効にしました` が表示される

#### `TC-D4-02`

- 観点: `ON -> OFF` 更新
- 前提: 現在 `ON`
- 手順:
  1. `制限を OFF にする` を押す
  2. 確認ダイアログで実行する
- 期待結果:
  - タイトル `レビュー閲覧制限を無効にしますか？` が表示される
  - 本文 `すべてのユーザーが、授業詳細ページのレビュー全文を閲覧できるようになります。` が表示される
  - 更新後に `レビュー閲覧制限を無効にしました` が表示される

#### `TC-D4-03`

- 観点: サーバー側更新失敗
- 前提: サーバー側更新を失敗させる
- 手順:
  1. 更新操作を行う
- 期待結果:
  - `レビュー閲覧制限の更新に失敗しました` が表示される

#### `TC-D4-04`

- 観点: 通信失敗
- 前提: 管理 API を通信失敗させる
- 手順:
  1. 更新操作を行う
- 期待結果:
  - `通信エラーが発生しました。時間をおいて再度お試しください` が表示される

## 8. 判定 D5: 公開側 API の返却

### 8.1 判定条件

- `C14`: `site_settings` レコードが存在する
- `C15`: `restriction_enabled = true`
- `C16`: ログイン済み
- `C17`: `reviews_count >= 1`
- `C18`: `reviews.length === 0`

### 8.2 C1 条件網羅の最小ケース

| ケースID | `site_settings` あり | 制限ON | ログイン済み | `reviews_count >= 1` | レビュー0件 | 想定結果 |
| --- | --- | --- | --- | --- | --- | --- |
| `TC-D5-01` | 偽 | 偽 | 偽 | 偽 | 偽 | `access.restriction_enabled = false`、`access.access_granted = true` |
| `TC-D5-02` | 真 | 真 | 偽 | 偽 | 偽 | `access.access_granted = false` |
| `TC-D5-03` | 真 | 真 | 真 | 偽 | 偽 | `access.access_granted = false` |
| `TC-D5-04` | 真 | 真 | 真 | 真 | 偽 | `access.access_granted = true` |
| `TC-D5-05` | 真 | 真 | 偽 | 偽 | 真 | `reviews: []` と `access` を返す |

### 8.3 テスト手順と期待結果

#### `TC-D5-01`

- 観点: `site_settings` 未作成時のフォールバック
- 前提: `site_settings` レコードなし
- 手順:
  1. `GET /api/v1/lectures/:lecture_id/reviews` を実行する
- 期待結果:
  - `access.restriction_enabled = false`
  - `access.access_granted = true`

#### `TC-D5-02`

- 観点: 制限 `ON`・未ログイン
- 前提: 制限 `ON`、未ログイン、`Lecture C`
- 手順:
  1. `GET /api/v1/lectures/:lecture_id/reviews` を実行する
- 期待結果:
  - `access.access_granted = false`
  - 1件目レビューは全文のまま返る
  - 2件目以降の本文は部分マスクされて返る

#### `TC-D5-03`

- 観点: 制限 `ON`・`reviews_count = 0`
- 前提: 制限 `ON`、`User Zero`、`Lecture C`
- 手順:
  1. 認証付きで `GET /api/v1/lectures/:lecture_id/reviews` を実行する
- 期待結果:
  - `access.access_granted = false`

#### `TC-D5-04`

- 観点: 制限 `ON`・`reviews_count >= 1`
- 前提: 制限 `ON`、`User One`、`Lecture C`
- 手順:
  1. 認証付きで `GET /api/v1/lectures/:lecture_id/reviews` を実行する
- 期待結果:
  - `access.access_granted = true`
  - 全レビュー本文が全文で返る

#### `TC-D5-05`

- 観点: レビュー 0 件でも `access` を返す
- 前提: 制限 `ON`、`Lecture A`
- 手順:
  1. `GET /api/v1/lectures/:lecture_id/reviews` を実行する
- 期待結果:
  - `reviews: []` が返る
  - `access` が必ず返る

## 9. 判定 D6: 管理側 API の返却

### 9.1 判定条件

- `C19`: 認証済み
- `C20`: 管理者
- `C21`: 更新値が妥当

### 9.2 C1 条件網羅の最小ケース

| ケースID | 認証済み | 管理者 | 更新値が妥当 | 想定結果 |
| --- | --- | --- | --- | --- |
| `TC-D6-01` | 偽 | 偽 | 真 | `401 Unauthorized` |
| `TC-D6-02` | 真 | 偽 | 真 | `403 Forbidden` |
| `TC-D6-03` | 真 | 真 | 真 | `200 OK` |
| `TC-D6-04` | 真 | 真 | 偽 | `422 Unprocessable Entity` |

### 9.3 テスト手順と期待結果

#### `TC-D6-01`

- 観点: 未ログインの管理 API アクセス
- 前提: 未ログイン
- 手順:
  1. `GET /api/v1/admin/review-access` を実行する
- 期待結果:
  - `401 Unauthorized`

#### `TC-D6-02`

- 観点: 非管理者の管理 API アクセス
- 前提: `General User`
- 手順:
  1. `GET /api/v1/admin/review-access` を実行する
- 期待結果:
  - `403 Forbidden`

#### `TC-D6-03`

- 観点: 管理者の設定取得・更新
- 前提: `Admin User`
- 手順:
  1. `GET /api/v1/admin/review-access` を実行する
  2. `PATCH /api/v1/admin/review-access` を妥当な値で実行する
- 期待結果:
  - 現在の `lecture_review_restriction_enabled` が返る
  - `updated_at` が返る
  - `last_updated_by` が返る
  - 更新後の値が返る

#### `TC-D6-04`

- 観点: 不正値更新
- 前提: `Admin User`
- 手順:
  1. 不正な値で `PATCH /api/v1/admin/review-access` を実行する
- 期待結果:
  - `422 Unprocessable Entity`

## 10. 補完ケース

C1 条件網羅の最小ケースに加えて、仕様保証のため以下を確認する。

### `TC-SUP-01`

- 観点: 最後の1件削除後の再ロック
- 前提: 制限 `ON`、`User One`、`Lecture C`
- 手順:
  1. `User One` が全文閲覧できることを確認する
  2. `User One` の唯一のレビューを削除する
  3. 同じ授業詳細を再表示する
- 期待結果:
  - `reviews_count = 0` になる
  - 再表示時は制限対象になる

### `TC-SUP-02`

- 観点: 制限 `OFF` 中のレビュー削除
- 前提: 制限 `OFF`、`User One`、`Lecture C`
- 手順:
  1. `User One` の唯一のレビューを削除する
  2. 同じ授業詳細を再表示する
- 期待結果:
  - `reviews_count = 0` になっても全文閲覧できる

### `TC-SUP-03`

- 観点: 制限中の `ありがとう`、`報告`、`ブックマーク`
- 前提: 制限 `ON`、制限対象ユーザー、`Lecture C`
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - `ありがとう` が表示される
  - `報告` が表示される
  - `ブックマーク` が表示される

### `TC-SUP-04`

- 観点: レビュー取得 API 失敗時の画面表示
- 前提: レビュー取得 API を失敗させる
- 手順:
  1. `/lectures/:id` を開く
- 期待結果:
  - レビュー取得エラーメッセージが表示される
  - 偽の `0件` / `0.0` 表示にならない

### `TC-SUP-05`

- 観点: 影響範囲外の画面
- 前提: 制限 `ON` と `OFF` を切り替える
- 手順:
  1. ルートページの最新レビューを確認する
  2. 授業一覧、検索結果を確認する
  3. レビュー投稿画面を確認する
  4. マイページ集計を確認する
- 期待結果:
  - いずれも表示仕様・動作仕様が変わらない

## 11. 最低限の実行セット

リリース前に最低限通すケースは以下とする。

- `TC-D1-01`
- `TC-D1-02`
- `TC-D1-03`
- `TC-D1-04`
- `TC-D2-01`
- `TC-D2-03`
- `TC-D3-01`
- `TC-D3-02`
- `TC-D3-03`
- `TC-D4-01`
- `TC-D4-02`
- `TC-D5-01`
- `TC-D5-02`
- `TC-D5-03`
- `TC-D5-04`
- `TC-D5-05`
- `TC-D6-01`
- `TC-D6-02`
- `TC-D6-03`
- `TC-D6-04`
- `TC-SUP-01`
- `TC-SUP-03`
